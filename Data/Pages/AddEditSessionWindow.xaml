<Window x:Class="ComputerTracker.Data.Pages.AddEditSessionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Добавить/Изменить сессию" Height="350" Width="500"
        WindowStartupLocation="CenterOwner" Background="#FF363636">
    <Grid Margin="10" Background="#FF363636">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Сотрудник -->
            <RowDefinition Height="Auto"/>
            <!-- Компьютер -->
            <RowDefinition Height="Auto"/>
            <!-- Начало сессии -->
            <RowDefinition Height="Auto"/>
            <!-- Окончание сессии -->
            <RowDefinition Height="*"/>
            <!-- Кнопки -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Выбор сотрудника -->
        <TextBlock Text="Сотрудник:" Grid.Row="0" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center" Foreground="White"/>
        <ComboBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"
              ItemsSource="{Binding Employees}"
              DisplayMemberPath="FullName"
              SelectedItem="{Binding SelectedEmployee}"/>

        <!-- Выбор компьютера -->
        <TextBlock Text="Компьютер:" Grid.Row="1" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center" Foreground="White"/>
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,5"
              ItemsSource="{Binding Computers}"
              DisplayMemberPath="ComputerName"
              SelectedItem="{Binding SelectedComputer}"/>

        <!-- Выбор даты и времени начала сессии -->
        <TextBlock Text="Начало:" Grid.Row="2" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center" Foreground="White"/>
        <xctk:DateTimePicker Grid.Row="2" Grid.Column="1" Margin="0,0,0,5"
                         Value="{Binding StartTime, Mode=TwoWay}"
                         Format="Custom"
                         FormatString="dd.MM.yyyy HH:mm:ss" />

        <!-- Выбор даты и времени окончания сессии (необязательно) -->
        <TextBlock Text="Окончание:" Grid.Row="3" Grid.Column="0" Margin="0,0,5,5" VerticalAlignment="Center" Foreground="White"/>
        <xctk:DateTimePicker Grid.Row="3" Grid.Column="1" Margin="0,0,0,5"
                     Value="{Binding EndTime, Mode=TwoWay}"
                     Format="Custom"
                     FormatString="dd.MM.yyyy HH:mm:ss"/>

        <!-- Панель кнопок OK/Отмена -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0" Height="30" VerticalAlignment="Bottom">
            <Button Content="OK" Width="70" Margin="0,0,10,0" Command="{Binding SaveCommand}" IsDefault="True"
              Background="#FF737373" Foreground="White" BorderBrush="#FF383838"/>
            <Button Content="Отмена" Width="70" IsCancel="True" Background="#FF737373" Foreground="White" BorderBrush="#FF383838"/>
        </StackPanel>
    </Grid>
</Window>
