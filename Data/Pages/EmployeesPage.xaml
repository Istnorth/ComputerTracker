<Page x:Class="ComputerTracker.Data.Pages.EmployeesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Сотрудники" Background="#FF3A3A3A">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Заголовок -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
            <RowDefinition Height="Auto"/>
            <!-- Панель кнопок -->
        </Grid.RowDefinitions>

        <TextBlock Text="Список сотрудников" FontSize="20" Foreground="White" Grid.Row="0" Margin="0,0,0,10"/>
        
        <DataGrid ItemsSource="{Binding Employees}" AutoGenerateColumns="False" 
              Grid.Row="1" Margin="0,0,0,10" Background="#FF363636"
              SelectedItem="{Binding SelectedEmployee}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="Auto"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="Auto"/>
                <DataGridTextColumn Header="Отчество" Binding="{Binding MiddleName}" Width="Auto"/>
                <DataGridTextColumn Header="Отдел" Binding="{Binding Department.DepartmentName}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Панель кнопок управления -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="10">
            <Button Content="Добавить" Margin="0,0,10,0" Command="{Binding AddEmployeeCommand}"/>
            <Button Content="Редактировать" Margin="0,0,10,0" Command="{Binding EditEmployeeCommand}"
              IsEnabled="{Binding SelectedEmployee, Converter={StaticResource NullToBooleanConverter}}"/>
            <Button Content="Удалить" Command="{Binding DeleteEmployeeCommand}"
              IsEnabled="{Binding SelectedEmployee, Converter={StaticResource NullToBooleanConverter}}"/>
        </StackPanel>
    </Grid>
</Page>
